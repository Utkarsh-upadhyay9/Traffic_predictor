#############################################
# Minimal requirements for Render deployment #
# (No native builds / No Rust toolchain)     #
#############################################

# NOTE: Render is ignoring runtime.txt and using Python 3.13.4
# We need to use versions that have prebuilt wheels for Python 3.13
# or fall back to pure Python alternatives

# FastAPI + Pydantic stack with guaranteed wheels
fastapi==0.104.1
pydantic==2.5.0
# Skip explicit pydantic-core - let pydantic choose compatible version

# ASGI server (pure Python, no compilation)
uvicorn==0.24.0

# Env & HTTP utilities (all pure Python)
python-dotenv==1.0.0
requests==2.31.0
python-dateutil==2.8.2

# (Intentionally no numpy / pandas / torch / scikit-learn)
# All ML functionality gracefully degrades to pattern-based logic.

# If later you restore DL models on a machine that supports wheels:
#   torch==2.1.2
#   scikit-learn==1.3.2
# Add ONLY when not on constrained build environments.

# End of file
