# Minimal requirements for Render.com deployment
# Optimized for fast builds and small memory footprint

# Core API (Fast install)
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-dotenv==1.0.0

# Essential ML (Pre-built wheels available)
numpy==1.24.3
scikit-learn==1.3.2
joblib==1.3.2

# Utilities
requests==2.31.0
holidays==0.35

# Optional: Uncomment if needed (increases build time)
# torch==2.1.2
# pandas==2.1.4
# google-generativeai==0.3.2
